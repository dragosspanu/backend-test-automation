query pokemon($name:String!) {
    pokemon(name: $name) {
        name
        weight{
            minimum
            maximum
        }
        height{
            minimum
            maximum
        }
    }
}